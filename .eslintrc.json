{
  "extends": ["next/core-web-vitals", "next/typescript"],
  "overrides": [
    {
      "files": ["**/*.test.ts", "**/*.test.tsx"], // Dotyczy tylko plików testowych
      "rules": {
        "@typescript-eslint/no-explicit-any": "off" // Wyłączenie reguły dla testów
      }
    }
  ]
}
